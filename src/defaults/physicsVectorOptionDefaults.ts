import { minMax } from '../types';

export const DEFAULT_GRAVITY = 0;
export const DEFAULT_MASS = 1;
export const DEFAULT_DISTANCE: minMax = { min: 5, max: 10 };
export const DEFAULT_DENSITY = 1;
export const DEFAULT_RESTITUTION = 1;